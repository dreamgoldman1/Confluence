import React from 'react'

// Components
import Tabs from './Tabs'

const SearchBox = () => {
  return (
    <section className='search-box'>
      <Tabs />
    </section>

  )
}

export default SearchBox